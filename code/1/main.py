#!/usr/bin/python
import utest
from saurabh import multiply

def add(firstNumber, secondNumber) :
	return firstNumber + secondNumber

print "Value of addition is ", add(4, 5)

print "Value of multiplication is ", multiply(4, 5)
